#include "main.h"
/**
 * add - a function that adds two integers and returns the result.
 * ch - first operand 
 * ch2 - second operand
 * 
 * Return: add the ch to ch2.
 */
int add(int ch, int ch2)
{
	return (ch + ch2);
}
