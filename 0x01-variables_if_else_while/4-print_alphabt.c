#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: (0)
 */
int main(void)
{
	char a, q, e;

	for (alp = 'a'; alp <= 'z'; alp++)
	{
	if (alp != e && alp != q)
	putchar(alp);
	}
	putchar('\n');
	return (0);
}
