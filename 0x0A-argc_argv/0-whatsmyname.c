#include "main.h"
#include <stdio.h>

/**
 * main - prints its name + \n
 * @argc: number of arguments
 * @argv: array of pointers
 *
 * Return: 0
 */
int main(int argc, char *argv[])
{
	int argc = 0;

	printf("%s\n", argv[argc]);
	return (0);
}
